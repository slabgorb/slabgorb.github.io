The directory for the MTV section is intended to hold images related to that category. Since you have not specified any images to include, I will create the directory structure without any files inside it.

The contents of the file /jekyll-website/jekyll-website/assets/images/mtv will be intentionally left blank. 

This file is intentionally left blank.